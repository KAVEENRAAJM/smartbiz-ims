{
  "name": "smartbiz-ims",
  "version": "1.0.0",
  "private": true,
  "description": "SmartBiz – Smart Inventory & Billing Management System",
  "scripts": {
    "setup": "npm install && cd client && npm install && cd ../server && npm install",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npx nodemon src/index.js",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "render-build": "cd client && npm install && npm run build && cd ../server && npm install",
    "render-start": "cd server && node src/index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
